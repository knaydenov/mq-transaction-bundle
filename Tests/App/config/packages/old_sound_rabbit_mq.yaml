old_sound_rabbit_mq:
    connections:
        default:
            url: 'amqp://guest:guest@rabbitmq:5672'
    producers:
        default:
            connection: default
            exchange_options: { name: 'default', type: 'topic' }
    consumers:
        default:
          connection:       default
          queue_options:    { name: 'default'}
          callback:         Kna\MQTransactionBundle\Tests\App\Consumer\DefaultConsumer

